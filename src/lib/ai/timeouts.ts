/** Per Claude tool-call window — one partition of a long chapter or manuscript. */
export const AI_WINDOW_TIMEOUT_MS = 210_000;

/** Single chapter / short manuscript call. */
export const AI_CHAPTER_TIMEOUT_MS = 280_000;
